/* Custom Global Variables & Reset */
:root {
    --bg-dark: #07070c;
    --apple-glow: rgba(255, 107, 107, 0.6);
    --text-main: #f5f5f7;
    --font-bengali: 'Hind Siliguri', sans-serif;
    --font-romantic: 'Galada', cursive;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    user-select: none;
    -webkit-user-select: none;
}

body {
    background-color: var(--bg-dark);
    color: var(--text-main);
    font-family: var(--font-bengali);
    overflow: hidden;
    height: 100vh;
    width: 100vw;
}

/* Background Canvas Layer */
#effect-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    pointer-events: none;
}

/* Layer Layout Architecture */
.layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 5;
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 1.2s cubic-bezier(0.25, 1, 0.5, 1), visibility 1.2s;
    padding: 20px;
}

.active-layer {
    opacity: 1;
    visibility: visible;
}

.hidden {
    display: none !important;
}

/* Premium Aurora Background Anim Effect */
.aurora-glow {
    position: absolute;
    width: 150%;
    height: 150%;
    background: radial-gradient(circle at 30% 20%, rgba(106, 17, 203, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(37, 117, 252, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, rgba(255, 75, 75, 0.1) 0%, transparent 60%);
    z-index: 2;
    animation: auroraMove 25s infinite alternate linear;
    pointer-events: none;
}

@keyframes auroraMove {
    0% { transform: translate(-10%, -10%) rotate(0deg); }
    100% { transform: translate(10%, 10%) rotate(10deg); }
}

/* LAYER 1: Loader Styling */
.loader-content {
    text-align: center;
    z-index: 10;
}

.heart-loader {
    font-size: 4rem;
    color: #ff4b5c;
    animation: pulse 1.2s infinite cubic-bezier(0.215, 0.610, 0.355, 1);
    text-shadow: 0 0 20px rgba(255, 75, 92, 0.6);
}

@keyframes pulse {
    0% { transform: scale(0.95); }
    50% { transform: scale(1.1); }
    100% { transform: scale(0.95); }
}

.loading-text {
    margin-top: 20px;
    font-size: 1.2rem;
    letter-spacing: 1px;
    opacity: 0.85;
}

.progress-bar-container {
    width: 260px;
    height: 4px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    margin: 20px auto 0;
    overflow: hidden;
}

.progress-bar {
    width: 0%;
    height: 100%;
    background: linear-gradient(90deg, #ff4b5c, #ff758c);
    box-shadow: 0 0 10px #ff4b5c;
    transition: width 0.1s linear;
}

/* Premium Buttons - Apple Style */
.btn-premium {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    padding: 14px 32px;
    font-family: var(--font-bengali);
    font-size: 1.1rem;
    color: white;
    border-radius: 30px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 10px;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.3);
    margin-top: 30px;
}

.btn-premium:hover {
    background: white;
    color: black;
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(255, 255, 255, 0.2);
}

.hidden-element {
    opacity: 0;
    transform: translateY(20px);
    pointer-events: none;
    transition: opacity 1s ease, transform 1s ease;
}

.visible-element {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
}

/* LAYER 2 & 3: Hero Section */
.hero-container {
    text-align: center;
    z-index: 10;
    max-width: 800px;
}

.typing-container h3 {
    font-size: 1.5rem;
    font-weight: 300;
    margin-bottom: 10px;
    opacity: 0.9;
}

.typing-container h1 {
    font-size: 3.5rem;
    font-family: var(--font-romantic);
    margin-bottom: 15px;
    letter-spacing: 2px;
}

.glow-text {
    text-shadow: 0 0 20px var(--apple-glow);
    color: #ff758c;
}

#interactive-moon {
    position: absolute;
    top: 10%;
    right: 10%;
    width: 80px;
    height: 80px;
    background: radial-gradient(circle at 30% 30%, #ffffff 0%, #e0e0e0 70%, #9e9e9e 100%);
    border-radius: 50%;
    box-shadow: 0 0 40px rgba(255, 255, 255, 0.4);
    cursor: pointer;
    z-index: 8;
    transition: transform 0.5s ease;
}

#interactive-moon:hover {
    transform: scale(1.1);
}

/* LAYER 5: 3D Gift Box Animation (CSS Only Mechanics) */
.gift-container {
    text-align: center;
    z-index: 10;
}

.section-title {
    font-size: 1.8rem;
    margin-bottom: 50px;
    font-weight: 400;
}

.gift-box {
    width: 150px;
    height: 150px;
    position: relative;
    margin: 0 auto;
    cursor: pointer;
    transform-style: preserve-3d;
    transition: transform 0.5s ease;
}

.gift-box:hover {
    transform: scale(1.05) rotateY(10deg);
}

.gift-lid {
    position: absolute;
    width: 160px;
    height: 40px;
    background: #ff4b5c;
    left: -5px;
    top: 0;
    z-index: 2;
    border-radius: 4px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    transition: transform 0.8s cubic-bezier(0.85, 0, 0.15, 1);
}

.gift-lid::after {
    content: '';
    position: absolute;
    width: 30px;
    height: 100%;
    background: #ff758c;
    left: 65px;
}

.gift-body {
    position: absolute;
    width: 150px;
    height: 120px;
    background: #d43f4e;
    bottom: 0;
    border-radius: 0 0 8px 8px;
}

.gift-body::after {
    content: '';
    position: absolute;
    width: 30px;
    height: 100%;
    background: #ff758c;
    left: 60px;
}

.gift-box.open .gift-lid {
    transform: translateY(-80px) rotate(-20deg);
    opacity: 0;
}

.gift-hint {
    margin-top: 30px;
    font-size: 1rem;
    opacity: 0.6;
    animation: blink 1.5s infinite;
}

@keyframes blink { 50% { opacity: 0.2; } }

/* LAYER 6: Glassmorphism Cinematic Slider */
.gallery-container {
    width: 100%;
    max-width: 900px;
    text-align: center;
    z-index: 10;
}

.section-title-premium {
    font-family: var(--font-romantic);
    font-size: 2.5rem;
    margin-bottom: 30px;
    color: #ff758c;
}

.slider-wrapper {
    position: relative;
    width: 100%;
    height: 480px;
    display: flex;
    justify-content: center;
    align-items: center;
}

.cinematic-slider {
    width: 100%;
    height: 100%;
    position: relative;
    perspective: 1000px;
}

.glass-card {
    position: absolute;
    width: 340px;
    height: 440px;
    left: calc(50% - 170px);
    top: 20px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(25px);
    -webkit-backdrop-filter: blur(25px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    padding: 15px;
    box-shadow: 0 30px 60px rgba(0,0,0,0.4);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    opacity: 0;
    transform: scale(0.8) translateZ(-200px);
    transition: transform 0.8s cubic-bezier(0.25, 1, 0.5, 1), opacity 0.8s ease;
    pointer-events: none;
}

/* Pure Original Image Preservations Styles */
.glass-card img {
    width: 100%;
    height: 80%;
    object-fit: cover; /* Resize and crop configuration safely */
    border-radius: 16px;
}

.card-caption {
    font-size: 1.05rem;
    padding: 10px 5px;
    font-weight: 300;
    opacity: 0.9;
    min-height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* Interactive Slider Z-Index States */
.glass-card.active {
    opacity: 1;
    transform: scale(1) translateZ(0);
    z-index: 15;
    pointer-events: auto;
}

.glass-card.prev {
    opacity: 0.4;
    transform: scale(0.85) translateX(-280px) rotateY(20deg);
    z-index: 10;
}

.glass-card.next {
    opacity: 0.4;
    transform: scale(0.85) translateX(280px) rotateY(-20deg);
    z-index: 10;
}

.slider-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    cursor: pointer;
    z-index: 20;
    transition: background 0.3s;
}

.slider-btn:hover { background: rgba(255, 255, 255, 0.25); }
.prev-btn { left: -20px; }
.next-btn { right: -20px; }
.container-center-btn { margin-top: 40px; }

/* LAYER 7: Luxury Love Letter Layout */
.letter-container-glass {
    width: 100%;
    max-width: 750px;
    z-index: 10;
    text-align: center;
}

.letter-paper {
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(30px);
    -webkit-backdrop-filter: blur(30px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: 40px 50px;
    border-radius: 24px;
    box-shadow: inset 0 0 20px rgba(255,255,255,0.02), 0 30px 60px rgba(0,0,0,0.5);
    text-align: left;
    max-height: 60vh;
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: rgba(255,255,255,0.2) transparent;
}

.letter-content-text {
    font-size: 1.15rem;
    line-height: 1.8;
    white-space: pre-line;
    font-weight: 300;
    letter-spacing: 0.5px;
    color: rgba(255, 255, 255, 0.95);
}

/* LAYER 8: Birthday Cake Architecture */
.cake-container {
    text-align: center;
    z-index: 10;
}

.cake-wrapper {
    position: relative;
    width: 250px;
    height: 200px;
    margin: 50px auto 0;
    cursor: pointer;
}

.cake-bottom, .cake-middle, .cake-top {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    background: linear-gradient(135deg, #ffd3b6, #ffaaa5);
    border-radius: 12px 12px 4px 4px;
    box-shadow: inset 0 5px 10px rgba(255,255,255,0.3), 0 10px 20px rgba(0,0,0,0.2);
}

.cake-bottom { width: 220px; height: 60px; bottom: 0; }
.cake-middle { width: 170px; height: 50px; bottom: 58px; background: linear-gradient(135deg, #ffaaa5, #ff8b94); }
.cake-top { width: 120px; height: 40px; bottom: 106px; background: linear-gradient(135deg, #ff8b94, #ff6b6b); }

.candle {
    position: absolute;
    width: 8px;
    height: 40px;
    background: repeating-linear-gradient(45deg, #ffffff, #ffffff 5px, #ff4b5c 5px, #ff4b5c 10px);
    left: calc(50% - 4px);
    bottom: 144px;
    border-radius: 4px 4px 0 0;
}

.flame {
    position: absolute;
    width: 16px;
    height: 24px;
    background: radial-gradient(circle at 50% 80%, #ffeb3b, #ff9800 60%, #f44336 100%);
    border-radius: 50% 50% 20% 20%;
    top: -26px;
    left: -4px;
    animation: flicker 0.1s infinite alternate;
    box-shadow: 0 0 20px #ff9800;
}

.flame.out { display: none; }

@keyframes flicker {
    0% { transform: scale(1); }
    100% { transform: scale(1.05) skewX(2deg); }
}

/* LAYER 10: Final Grand Layout Container */
.glass-card-final {
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(40px);
    -webkit-backdrop-filter: blur(40px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    padding: 60px 80px;
    border-radius: 32px;
    text-align: center;
    box-shadow: 0 40px 100px rgba(0,0,0,0.6);
    z-index: 10;
}

.glow-text-intense {
    font-family: var(--font-romantic);
    font-size: 4.5rem;
    color: #white;
    text-shadow: 0 0 30px #ff4b5c, 0 0 60px #ff758c;
    margin-bottom: 25px;
    animation: titleFloat 4s ease-in-out infinite alternate;
}

@keyframes titleFloat {
    0% { transform: translateY(0); }
    100% { transform: translateY(-10px); }
}

.grand-subtitle {
    font-size: 1.5rem;
    font-weight: 300;
    opacity: 0.9;
    letter-spacing: 1px;
}

.signature {
    display: block;
    margin-top: 40px;
    font-family: var(--font-romantic);
    font-size: 2rem;
    color: #ff758c;
}

.restart-hint {
    margin-top: 30px;
    font-size: 0.9rem;
    opacity: 0.4;
    text-align: center;
    z-index: 10;
}

/* Easter Egg Toast Notification system */
.toast {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%) translateY(0);
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 12px 30px;
    border-radius: 20px;
    font-size: 0.95rem;
    z-index: 100;
    transition: all 0.5s ease;
    opacity: 0;
}

.toast.show {
    opacity: 1;
    transform: translateX(-50%) translateY(-10px);
}

/* Mobile Responsiveness Rules via Media Queries */
@media (max-width: 768px) {
    .typing-container h1 { font-size: 2.3rem; }
    .typing-container h3 { font-size: 1.1rem; }
    .glass-card { width: 280px; height: 380px; left: calc(50% - 140px); }
    .slider-btn { display: none; } /* Swiping / tapping control optimized on mobile */
    .letter-paper { padding: 25px 20px; }
    .letter-content-text { font-size: 1.05rem; }
    .glow-text-intense { font-size: 2.8rem; }
    .grand-subtitle { font-size: 1.1rem; }
    .glass-card-final { padding: 40px 20px; }
}
